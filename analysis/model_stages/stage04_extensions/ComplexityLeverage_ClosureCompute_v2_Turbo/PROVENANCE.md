# Provenance

`frozen_stage4/src/stage4_gpu_simulator.py`, `frozen_stage4/src/stage4_topology.py`, and `frozen_stage4/stage4a.json` were copied from the exact `source_snapshot/` embedded in the completed Stage4F closure results archive. Cross-state transfer therefore changes only the evaluation state and fresh noise seeds; it does not change the Stage4 cellular/network simulator.

The Allen lag-aware refit preserves the Stage5B leverage form `activity amplitude × latent dynamical direction`, but strengthens model selection and quality control with blocked lag selection, an untouched final test block, and a persistence baseline.
