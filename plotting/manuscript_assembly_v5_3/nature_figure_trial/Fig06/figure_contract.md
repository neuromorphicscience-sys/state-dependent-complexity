# Fig. 6 reference-layout contract

- Nature double-column width: 183 mm.
- Canvas: 183 x 107 mm; four plotting rows with a shared 15 mm axis height.
- Reading order: `a+a+b+b / c+c+d+d / e+e+f+f / g+h+h+i`.
- The second b microplot promotes the ED7 Allen quality-gated stability control, testing whether residual reconfiguration is explained by poor latent-model fits.
- No multi-plot panel wraps across rows.
- Panel f displays AUC excess above chance; this is exactly raw AUC minus 0.5, so the omitted duplicate axis loses no information. Both source tables remain mapped.
- Panel g combines three dimensionless confound metrics while retaining every mouse-level observation and each metric's own label.
- The former non-data schematic panel j is removed; the figure ends with formal synthesis panel i.
- Visible panel identifiers use only bold lowercase a-i; internal microplot mappings remain in the script and manifest.
- This is a review/reference version and is not frozen.
