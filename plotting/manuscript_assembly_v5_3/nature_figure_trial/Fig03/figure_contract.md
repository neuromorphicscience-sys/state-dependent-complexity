# Fig3 source-data-first Nature reference contract

- Final reference canvas: 183 x 108 mm.
- All quantitative axes have the same 24 mm physical y-axis height.
- Row order: a+b pair+c / d pair+e / f four-topology strip.
- Panel b remains an indivisible two-microplot group.
- Panel c remains directly after b because fresh-validation separation is the immediate guardrail for the validated dynamics-aware advantage.
- Panel d remains an indivisible two-microplot group; both axes are widened from the initial 36 mm to 42 mm and use upper-left in-axes legends.
- Panel e shares the d row and uses an in-axes upper-right legend; reserved headroom prevents it from covering the discovery estimate.
- Adjacent quantitative rows use approximately 10 mm of vertical axis-to-axis whitespace.
- Panel f is rebuilt as four independent source-data-driven axes in one row. The legacy pre-arranged 2 x 2 image is never pasted into the composite.
- Fig1/Fig2 semantic colors are retained: dynamics-aware teal, spectral/replication blue, structural support purple/green/coral, and random gray.
- Panel a is a native 34 x 20.5 mm vector schematic contrasting topology-only
  prominence with independently validated dynamical leverage in the same graph.
  It introduces no measured values and uses the locked purple/teal/blue palette.
- Outputs: embedded-font PDF, editable-text SVG, and 600 dpi PNG/TIFF.
- Every quantitative mark is read from the frozen V5.3 Fig3 source-data tables; the discovery point in d uses the archived paired-gain summary.
